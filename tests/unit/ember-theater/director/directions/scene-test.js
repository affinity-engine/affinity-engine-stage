import Scene from 'ember-theater-director/ember-theater/director/directions/scene';
import { module, test } from 'qunit';

module('Unit | Ember Theater Direction | scene');

// Replace this with your real tests.
test('it works', function(assert) {
  var scene = Scene.create();
  assert.ok(scene);
});
