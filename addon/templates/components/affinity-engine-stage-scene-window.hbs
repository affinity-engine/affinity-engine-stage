{{#if screen}}
  <div class="ae-stage-scene-window-screen {{joinedScreenClassNames}}" style={{childStyle}} data-test={{hook "affinity_engine_stage_scene_window_screen"}}></div>
{{/if}}

<div class="ae-stage-scene-window-main {{joinedConfigurableClassNames}}" style={{childStyle}} data-test={{hook "affinity_engine_stage_scene_window_main"}}>
  {{affinity-engine-stage
    initialScene=sceneId
    sceneRecord=sceneRecord
    engineId=engineId
    windowId=sceneWindowId
    window=window
  }}
</div>
